name: Tests

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

jobs:
  test:
    uses: mjtomei/omerta/.github/workflows/test-python.yml@master
    with:
      package-path: '.'
      package-name: 'omerta-lang'
      test-path: 'tests/'
